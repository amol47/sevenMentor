import { UsertablecolumnkeyPipe } from './usertablecolumnkey.pipe';

describe('UsertablecolumnkeyPipe', () => {
  it('create an instance', () => {
    const pipe = new UsertablecolumnkeyPipe();
    expect(pipe).toBeTruthy();
  });
});
